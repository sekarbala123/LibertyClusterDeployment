<server description="WebSphere Liberty Cluster Member 2">

    <featureManager>
        <feature>adminCenter-1.0</feature>
        <feature>restConnector-2.0</feature>
        <feature>collectiveMember-1.0</feature>
    </featureManager>

    <!-- Different ports from controller and member1 -->
    <httpEndpoint id="defaultHttpEndpoint"
                  host="*"
                  httpPort="9082"
                  httpsPort="9445" />

    <!-- Quick Start Security for Admin Center -->
    <quickStartSecurity userName="admin" userPassword="adminpwd" />

    <!-- Define the host name for use by the collective -->
    <variable name="defaultHostName" value="localhost" />

    <!-- Collective Member Configuration -->
    <collectiveMember controllerHost="localhost"
                      controllerPort="9443" />

    <!-- SSL Configuration with client authentication for collective -->
    <ssl id="defaultSSLConfig"
         keyStoreRef="defaultKeyStore"
         trustStoreRef="defaultTrustStore"
         clientAuthenticationSupported="true"
         verifyHostname="false"
         sslProtocol="TLSv1.2"/>

    <!-- inbound (HTTPS) keystore -->
    <keyStore id="defaultKeyStore" password="{xor}EzY9Oi0rJg=="
              location="${server.config.dir}/resources/security/key.p12" />

    <!-- inbound (HTTPS) truststore -->
    <keyStore id="defaultTrustStore" password="{xor}EzY9Oi0rJg=="
              location="${server.config.dir}/resources/security/trust.p12" />

    <!-- server identity keystore -->
    <keyStore id="serverIdentity" password="{xor}EzY9Oi0rJg=="
              location="${server.config.dir}/resources/collective/serverIdentity.p12" />

    <!-- collective truststore -->
    <keyStore id="collectiveTrust" password="{xor}EzY9Oi0rJg=="
              location="${server.config.dir}/resources/collective/collectiveTrust.p12" />

    <!-- Remote file access for collective operations -->
    <remoteFileAccess>
        <readDir>${wlp.install.dir}</readDir>
        <readDir>${wlp.user.dir}</readDir>
        <readDir>${server.output.dir}</readDir>
        <readDir>${server.config.dir}</readDir>
        <writeDir>${server.output.dir}</writeDir>
    </remoteFileAccess>

</server>
